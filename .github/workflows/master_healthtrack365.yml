# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - healthtrack365

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://index.docker.io/v1/
        username: ${{ secrets.AzureAppService_ContainerUsername_fdc98a875e044845b73c365b5dbadfa8 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_803fdd24956347dda3911878e9e9d98f }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: index.docker.io/${{ secrets.AzureAppService_ContainerUsername_fdc98a875e044845b73c365b5dbadfa8 }}/kirankarandikar/healthtrack365:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'healthtrack365'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_a9d8262d4eff413a8bcdba0f37f2f465 }}
        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_fdc98a875e044845b73c365b5dbadfa8 }}/kirankarandikar/healthtrack365:${{ github.sha }}'